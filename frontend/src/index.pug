doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        header 
            h1.logo ZAYMAX 
        main.container
            .seq 
            .form 
                form.postCreate
                    include ./include/bio.pug
                    include ./include/connect.pug
                    include ./include/job.pug
                    button#jobCreate(type="button") Job +
                    